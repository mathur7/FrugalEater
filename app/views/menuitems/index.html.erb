

<div class="panel panel-default">
  <div class="panel-heading">Here's all the stuff you can eat, frugaleater!</div>
  <table class="table table-hover">

    <thead>
    <tr>
    <th>Name</th>
    <th>Price</th>
    <th>Restaurant</th>
    </tr>
    </thead>

    <tbody>
    <!-- I could be wrong, but it seems like the loop is off here.  
         don't you want the tr in each iteration of the loop? -->
    <tr>    
        <% @menuitems["objects"].each do |i| %>
        <td><a href='/menuitems/<%=i["id"]%>'><%=i["name"]%></a></td>
        <td><a href='/menuitems/<%=i["id"]%>'><%=i["price"]%></a></td>
        <td><a href='/menuitems/<%=i["id"]%>'><%=i["venue"]["name"]%></a><br></td>
        </tr>
    </tbody> 
    <% end %>

    </table> 
</div> 
