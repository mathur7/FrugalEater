<div class = "header"> Hungry? But can't afford to spend a bomb? No sweat! Frugal eater is here to your rescue! </div>
<div class = "row1">
	<div class="col-md-3">

			<div class="border-form-div">
				<div id = "user_nav">
				<%= form_tag("/menuitems", method: "get") do %>
					<p><%= label_tag(:name, "What food item are you craving today?") %></p>
					<%= text_field_tag(:name) %><br></br>
					<%= submit_tag("Search", :class => "btn btn-primary") %><br></br>
				<% end %>
				<div id = "powered_by_locu"><a href="http://locu.com"><img height="25" width="155" src="http://i.imgur.com/I2mkQba.png"></a></div>

				</div>
			</div>

			<div class="border-form-div">
				<div id = "user_nav">
					<% if user_signed_in? %>
						Signed in as <%= current_user.email %>. Not you?
						<%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
					<% else %>
						<p> New user? Sign up now! </p>
						<p><%= link_to "Sign up", new_user_registration_path,:class=>'btn btn-info' %></p>
						<p> Already a member? Sign in here! </p>
						<p><%= link_to "Sign in", new_user_session_path,:class=>'btn btn-info' %></p>
					<% end %>
				</div>
			</div>


			<% flash.each do|key,value| %>
   			<div class = "flash"> <%=value%> </div>
			<% end %>
	</div>
</div>
    <!-- I like the use of a partial here -->
		<%= render "home/slideshow" %>